{
  "name": "flyrank-capstone-image-relevance",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "migrate": "node --env-file-if-exists=.env scripts/migrate.js",
    "seed": "node --env-file-if-exists=.env scripts/seed.js",
    "corpus": "node scripts/download-corpus.js",
    "evaluate": "node --env-file-if-exists=.env scripts/evaluate.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "test:browser": "node scripts/browser-proof.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "multer": "^2.0.2",
    "pg": "^8.16.0",
    "sharp": "^0.35.4",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  },
  "author": {
    "name": "Peter Maged",
    "url": "https://petermaged.com/"
  },
  "license": "MIT"
}
